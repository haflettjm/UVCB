services:
  nats:
    image: nats:latest
    ports:
      - "4222:4222"
    restart: unless-stopped

  uvcb:
    build: .
    depends_on:
      - nats
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
